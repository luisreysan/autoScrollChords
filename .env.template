# Turso (libSQL) — create a database at https://turso.tech/
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Upstash Redis REST (recommended)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional compatibility aliases (you can leave these empty if UPSTASH_* are set)
KV_REST_API_URL=
KV_REST_API_TOKEN=
